# ThetaIota Configuration Example
# Copy this file to .env and customize as needed

# Server Configuration
RA_BIND_HOST=127.0.0.1
RA_PORT_BASE=8081
RA_REPLICAS=3

# Database Configuration
# RA_DB_PATH=/path/to/agent_A.db  # Optional - defaults computed if unset
# RA_CHECKPOINT_DIR=/path/to/checkpoints  # Optional - defaults to ./checkpoints

# Security (Production)
# RA_AUTH_SECRET=your-long-random-secret-here  # Enables bearer token auth
RA_RATE_LIMIT=20/minute

# CORS (Optional)
# RA_CORS=http://localhost:3000,https://yourdomain.com

# Guardian Rules (Optional)
# RA_RULES=/path/to/guardian/rules.yaml
